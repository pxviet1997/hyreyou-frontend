const Logo = (props) => (
  <img
    alt="Logo"
    src="/static/images/logo-big.png"
    style={{
      backgroundColor: 'white',
      height: 64
    }}
    {...props}
  />
);

export default Logo;
